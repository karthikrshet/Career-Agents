# Mode: Add Application Entry (`add.md`)

Add target opportunity to the live application tracker:
1. Validate required fields: company, role, posting URL, date.
2. Initialize status to `Identified` or `Applied`.
3. Reconcile against duplicate entries in `pipeline-tracker.md`.
